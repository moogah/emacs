# Construct Tests

Tests for bash language constructs (loops, conditionals, heredocs, directory contexts).

Contents: Loop context, conditional context, heredoc context, directory-changing commands, pwd context tests.
