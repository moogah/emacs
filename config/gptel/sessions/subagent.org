#+title: GPTEL Session Subagent Helpers
#+author: Jeff Farr
#+property: header-args:emacs-lisp :tangle subagent.el
#+auto_tangle: y

* Introduction

Subagent session management module.

All subagent functionality is now handled by:
- Subagent creation: =config/gptel/tools/persistent-agent.el=
- Parent-child linking: via scope-plan.yml parent_session_id field

This module is retained for backward compatibility but contains no active code.

* Lexical Binding

#+begin_src emacs-lisp
;;; subagent.el --- GPTEL Subagent Session Helpers -*- lexical-binding: t; -*-

;; Copyright (C) 2024-2026 Jeff Farr

;;; Commentary:

;; Subagent session management module.
;; All functionality now handled by persistent-agent.el and scope-plan.yml.
;; Retained for backward compatibility.

;;; Code:
#+end_src


* Provide Feature

#+begin_src emacs-lisp
(provide 'gptel-session-subagent)
;;; subagent.el ends here
#+end_src
