#+title: GPTEL Chat Mode - Turn Navigation
#+author: Jeff Farr
#+property: header-args:emacs-lisp :tangle nav.el :comments both
#+auto_tangle: y

* Introduction

Scaffolded stub for =gptel-chat-nav= — navigation commands
(=gptel-chat-next-turn=, =gptel-chat-previous-turn=) and
=gptel-chat-regenerate= (remove last assistant block and re-send).

See =openspec/changes/gptel-chat-mode/architecture.md= §Components
(subsection =gptel-chat-nav (navigation)=) for the full contract.

* Lexical Binding

#+begin_src emacs-lisp :comments no
;;; nav.el --- GPTEL Chat-Mode Turn Navigation (stub) -*- lexical-binding: t; -*-

;; Copyright (C) 2024-2026 Jeff Farr

;;; Commentary:

;; Scaffolded stub for chat-mode turn navigation and regenerate.
;; Populated by a later task in the gptel-chat-mode OpenSpec change.

;;; Code:
#+end_src

* Provide

#+begin_src emacs-lisp
;; TODO(gptel-chat-mode): implement `gptel-chat-next-turn',
;; `gptel-chat-previous-turn', and `gptel-chat-regenerate'.  Tracked
;; by the gptel-chat-mode change.

(provide 'gptel-chat-nav)

;;; nav.el ends here
#+end_src
