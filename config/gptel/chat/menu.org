#+title: GPTEL Chat Mode - Menu and Preset Wiring
#+author: Jeff Farr
#+property: header-args:emacs-lisp :tangle menu.el :comments both
#+auto_tangle: y

* Introduction

Scaffolded stub for =gptel-chat-menu= — the transient prefix that
reuses =gptel-menu='s configuration infixes with the Send suffix
rebound to =gptel-chat-send=, plus preset-application wiring applied
at chat-mode activation.

See =openspec/changes/gptel-chat-mode/architecture.md= §Components
(subsection =gptel-chat-menu (menu, preset-wiring)=) for the full
contract. Note: this module does *not* enable =gptel-mode= — chat-mode
owns the buffer role exclusively (design.md Decision 16).

* Lexical Binding

#+begin_src emacs-lisp :comments no
;;; menu.el --- GPTEL Chat-Mode Menu and Preset Wiring (stub) -*- lexical-binding: t; -*-

;; Copyright (C) 2024-2026 Jeff Farr

;;; Commentary:

;; Scaffolded stub for the chat-mode transient menu and
;; preset-application hook.  Populated by a later task in the
;; gptel-chat-mode OpenSpec change.

;;; Code:
#+end_src

* Provide

#+begin_src emacs-lisp
;; TODO(gptel-chat-mode): implement `gptel-chat-menu' transient prefix
;; and the preset-application hook that runs at chat-mode activation.
;; Tracked by the gptel-chat-mode change.

(provide 'gptel-chat-menu)

;;; menu.el ends here
#+end_src
