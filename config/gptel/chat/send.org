#+title: GPTEL Chat Mode - Send Command
#+author: Jeff Farr
#+property: header-args:emacs-lisp :tangle send.el :comments both
#+auto_tangle: y

* Introduction

Scaffolded stub for =gptel-chat-send= — the interactive command
bound to =C-c C-c= that validates preconditions, constructs the
message list via the parser, invokes =gptel-request=, and manages
the open-assistant-block lifecycle.

See =openspec/changes/gptel-chat-mode/architecture.md= §Components
(subsection =gptel-chat-send (send)=) for the full contract.

* Lexical Binding

#+begin_src emacs-lisp :comments no
;;; send.el --- GPTEL Chat-Mode Send Command (stub) -*- lexical-binding: t; -*-

;; Copyright (C) 2024-2026 Jeff Farr

;;; Commentary:

;; Scaffolded stub for the chat-mode send command.  Populated by a
;; later task in the gptel-chat-mode OpenSpec change.

;;; Code:
#+end_src

* Provide

#+begin_src emacs-lisp
;; TODO(gptel-chat-mode): implement `gptel-chat-send' (interactive),
;; send-precondition validation, and assistant-block lifecycle
;; management.  Tracked by the gptel-chat-mode change.

(provide 'gptel-chat-send)

;;; send.el ends here
#+end_src
