#+title: GPTEL Chat Mode - Delimiter Sanitization
#+author: Jeff Farr
#+property: header-args:emacs-lisp :tangle sanitize.el :comments both
#+auto_tangle: y

* Introduction

Scaffolded stub for =gptel-chat-sanitize= — delimiter-collision
escaping utilities used by the streaming module and the parser's
reverse un-escape path.

See =openspec/changes/gptel-chat-mode/architecture.md= §Components
(subsection =gptel-chat-stream= — sanitization is a sibling helper
used by =stream=): line-by-line escape of
=^#\+end_\(user\|assistant\|tool\)\b= to =,#+end_...= with
partial-line holdback across streaming chunks.

* Lexical Binding

#+begin_src emacs-lisp :comments no
;;; sanitize.el --- GPTEL Chat-Mode Sanitization Helpers (stub) -*- lexical-binding: t; -*-

;; Copyright (C) 2024-2026 Jeff Farr

;;; Commentary:

;; Scaffolded stub for chat-mode delimiter sanitization.  Populated by
;; a later task in the gptel-chat-mode OpenSpec change.

;;; Code:
#+end_src

* Provide

#+begin_src emacs-lisp
;; TODO(gptel-chat-mode): implement `gptel-chat--sanitize-chunk' and
;; companion un-escape used by the parser.  Tracked by the
;; gptel-chat-mode change.

(provide 'gptel-chat-sanitize)

;;; sanitize.el ends here
#+end_src
