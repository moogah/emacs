#+title: GPTEL Chat Mode - Streaming Callback
#+author: Jeff Farr
#+property: header-args:emacs-lisp :tangle stream.el :comments both
#+auto_tangle: y

* Introduction

Scaffolded stub for =gptel-chat-stream= — the streaming callback
passed to =gptel-request= and the state machine that accumulates
partial lines, sanitizes chunks, and renders tool-call events as
nested =#+begin_tool= blocks.

See =openspec/changes/gptel-chat-mode/architecture.md= §Components
(subsection =gptel-chat-stream (stream)=) for the full contract.

* Lexical Binding

#+begin_src emacs-lisp :comments no
;;; stream.el --- GPTEL Chat-Mode Streaming (stub) -*- lexical-binding: t; -*-

;; Copyright (C) 2024-2026 Jeff Farr

;;; Commentary:

;; Scaffolded stub for the chat-mode streaming callback.  Populated by
;; a later task in the gptel-chat-mode OpenSpec change.

;;; Code:
#+end_src

* Provide

#+begin_src emacs-lisp
;; TODO(gptel-chat-mode): implement `gptel-chat--install-stream-callback'
;; closure, chunk accumulator, abort handling, and tool-call block
;; rendering.  Tracked by the gptel-chat-mode change.

(provide 'gptel-chat-stream)

;;; stream.el ends here
#+end_src
