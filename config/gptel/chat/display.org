#+title: GPTEL Chat Mode - Display Layer
#+author: Jeff Farr
#+property: header-args:emacs-lisp :tangle display.el :comments both
#+auto_tangle: y

* Introduction

Scaffolded stub for =gptel-chat-display= — the presentation layer
that applies distinct faces (or =line-prefix= overlays) to user and
assistant block content, plus the =gptel-chat-toggle-display-layer=
command.

See =openspec/changes/gptel-chat-mode/architecture.md= §Components
(subsection =gptel-chat-display (display)=) for the full contract.

* Lexical Binding

#+begin_src emacs-lisp :comments no
;;; display.el --- GPTEL Chat-Mode Display Layer (stub) -*- lexical-binding: t; -*-

;; Copyright (C) 2024-2026 Jeff Farr

;;; Commentary:

;; Scaffolded stub for chat-mode display presentation layer.
;; Populated by a later task in the gptel-chat-mode OpenSpec change.

;;; Code:
#+end_src

* Provide

#+begin_src emacs-lisp
;; TODO(gptel-chat-mode): implement faces/overlays for user- and
;; assistant-block content and `gptel-chat-toggle-display-layer'.
;; Tracked by the gptel-chat-mode change.

(provide 'gptel-chat-display)

;;; display.el ends here
#+end_src
