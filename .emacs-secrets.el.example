;; Example secrets file for isolated Emacs configuration
;; Copy this file to ~/.emacs-secrets.el and fill in your actual values
;;
;; DO NOT commit ~/.emacs-secrets.el to version control!
;;
;; Usage:
;;   cp .emacs-secrets.el.example ~/.emacs-secrets.el
;;   emacs ~/.emacs-secrets.el  # Edit with your credentials

;; -*- lexical-binding: t; -*-

;; ===============================================================================
;; PostgreSQL Credentials
;; ===============================================================================
;; Used by sql-postgres mode for database connections

(setq jf/postgres-server "your-server.example.com"
      jf/postgres-port "5432"
      jf/postgres-database "your_database"
      jf/postgres-user "your_username")

;; ===============================================================================
;; GPG Configuration
;; ===============================================================================
;; Email address for GPG encryption/signing

(setq jf/gpg-email "your-email@example.com")

;; ===============================================================================
;; Additional Secrets
;; ===============================================================================
;; Add any other sensitive configuration variables here
;; Examples:
;;
;; API keys:
;; (setq jf/openai-api-key "sk-...")
;; (setq jf/github-token "ghp_...")
;;
;; Credentials:
;; (setq jf/smtp-password "...")
;; (setq jf/auth-sources '("~/.authinfo.gpg"))
