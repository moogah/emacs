#+title: Working-Directory Scoping for gptel Sessions & Agents — Exploration Log
#+author: Jeff Farr
#+startup: overview
#+todo: THREAD(t) PARKED(p) | RESOLVED(d) SEPARABLE(s)

# STATUS:   exploration (NOT a design decision — see "How to use" below)
# SOURCE:   .tasks/design-working-directory-scoping-for-sessions-and-agents.md
#           (surfaced 2026-06-13 during first live PersistentAgent test, commit 3f7feee)
# CREATED:  2026-06-13 explore session
# UPDATED:  2026-06-19 explore session (Threads D AND E RESOLVED.
#           D: home.org leg DROPPED -> env builder is 100% workspace-neutral;
#           env block at TAIL of the composed system message via the existing
#           pre-send seam (covers chat AND agents); idempotent wholesale rebuild;
#           agent write path untouched; worktree list dropped from scope.
#           E: agent leg found ALREADY IMPLEMENTED (archive 2026-06-18-gptel-
#           work-root-default-directory, D5/D6/D7); env block (D) is the agent's
#           cwd/scope channel since agents are gptel-chat-mode buffers; write-
#           perms taken as an assumption (separate small fix; see write-axis
#           diagnosis); worktree manifest the lone deferred item.
#           See [[*RESOLVED D]], [[*RESOLVED E]].
#           2026-06-17 explore session (Thread B RESOLVED: design locked
#           + load-bearing spike CONFIRMED that chat AND agent tool calls
#           run with the originating buffer's default-directory, so the
#           mode-hook binder is the single binding point; Thread E agent
#           leg scoped: parent PASSES work_root + read/write paths, /tmp
#           is scratch not the write default. Earlier: Thread C RESOLVED
#           dir-locals redundant vs the integration-registry payload; see
#           [[*RESOLVED B]], [[*THREAD E]], [[*RESOLVED C]])
# RELATES:  .tasks/done/refactor-request-scope-expansion-to-take-operation.md
#           .tasks/harden-persistent-agent-callback-on-error.md
#           openspec/specs/gptel/scope.md, openspec/specs/gptel/persistent-agent.md

* How to use this document

This is the captured output of a deep =/opsx-explore= session. It is
*not* a finalized design and deliberately does not crystallize on one.
The exploration converged on a guiding *principle* (see [[*Emerging design principle]])
but also established that we are dealing with *several concurrent
problems* that should each get their own follow-up session.

Each entry under [[*Threads (follow-up session seeds)]] is written to be
liftable: copy the thread's heading + body as the opening prompt of a
new session. Part 1 (current-state findings) is shared context every
thread will want; reference it rather than re-deriving.

Hard constraint that shapes every thread: *gptel's default-directory /
work-root setup MUST be operable independently of the workspaces
package* (one-way dependency; workspaces never names gptel, and gptel
must work with workspaces absent). See [[*Cross-cutting constraints & invariants]].

A standing reminder from this session: *this gptel codebase is beta.*
It carries abandoned implementations. Findings below are "behavior as
it exists today," verified to file:line — but existing behavior is
*evidence, not canon*. Do not treat any current mechanism as a baseline
that must be preserved.

* Part 1 — Current-state findings (grounded)

** The three-role model (the core lens)

A "directory" plays three distinct roles in a gptel session. Today only
one is named explicitly; a second is conflated into it by accident; the
third is computed and discarded.

#+begin_example
  Role 1  SESSION / METADATA ROOT   bookkeeping dir (session.org, system-prompt, drawer)
          - tracked explicitly as buffer-local jf/gptel--branch-dir
          - scope drawer is LOADED from here
  Role 2  WORK ROOT                  what cwd / relative paths SHOULD mean (the project)
          - should be `default-directory`; should be what the model is TOLD
          - TODAY: unset -> falls through to Role 1 (the bug)
  Role 3  SCOPE BOUNDARY             GPTEL_SCOPE_READ/WRITE/DENY globs
          - already cleanly separate, self-describing in the drawer
#+end_example

The whole bug class reduces to: *Role 2 is never set, so `default-directory`
silently falls through to Role 1.* Both chat sessions and agents open
their buffer with =find-file-noselect= on a file under the session tree,
and nobody sets =default-directory= afterward.

** The cwd bug's true nature: wrong path universe, NOT fail-open

Originally framed (in the .tasks item) as "scope is bypassed / ineffective."
Closer reading shows the opposite mechanism:

- With =default-directory= = the sandbox/bookkeeping dir, a *relative*
  path the model emits resolves *inside that dir*, not the project.
- The validator then judges a path that has nothing to do with what the
  model meant. The scope decision becomes *meaningless* — sometimes
  spuriously permissive, sometimes spuriously restrictive — never "about
  the right file."

#+begin_example
  model intends:   read project file  config/gptel/presets/minimal.md
  resolved vs cwd= sandbox:  ~/.gptel/sessions/.../<agent>/config/gptel/presets/minimal.md
  validator judges THAT path  ->  decision is about a file the model never meant
#+end_example

Implication: fixing =default-directory= is not a convenience — it is what
makes "the path the validator judges" equal "the path the model meant."
Only then is scope validation testable or meaningful. The
file_not_found-vs-scope_violation ordering question is downstream of this.

** Scope validation is fail-CLOSED in current code (with a branch caveat)

=jf/gptel-scope--load-config= (=config/gptel/scope/scope-validation.org:724-759=):
nil config, empty drawer, missing =session.org=, *or any thrown error* all
return =jf/gptel-scope--deny-all-defaults= (every pattern list nil -> every
path denies). No fail-open path exists. Reads like recent, deliberate hardening.

The agent loads its *own* drawer: =jf/gptel--auto-init-session-buffer=
(=commands.org:335=) sets buffer-local =jf/gptel--branch-dir= to the agent's
dir; tool callbacks run inside =(with-current-buffer agent-buffer ...)=
(=persistent-agent.el:370=), so =load-config= reads the agent's drawer.

*Caveat / discrepancy worth a branch-diff:* the .tasks worked example
(file_not_found where current code would yield scope_violation) was
captured in worktree =~/emacs-persistent-agent-review=; we are reading
=~/emacs-gptel-default-directory=. The fail-closed =deny-all-defaults=
machinery appears to post-date that test. *Do not trust the worked
example's four rows as the current baseline without re-running on this branch.*

Relevant decision logic:
- =jf/gptel-scope--validate-path-operation= (=scope-validation.org:147-219=):
  =:read= allowed if path matches read OR write patterns; =:write= requires
  write patterns. Deny patterns checked first.
- =jf/gptel-scope--glob-match-p= (=scope-validation.org:123=): =string-match-p=
  over a glob->regex. (Check anchoring if substring false-matches ever suspected.)

** Two surprises confirmed

1. *=tools.org= is dead code.* =jf/gptel-session--tools-log-file= constant
   (=constants.org:77=) + accessor =jf/gptel--tools-log-path=
   (=filesystem.org:175=) exist but have *zero call sites*. Never wired up;
   not a deprecation. Also dead: =system-prompts.org= constant
   (=constants.org:81=). Clean deletion — see Thread H (janitorial, separable).

2. *There is no ={session_dir}/**= scope glob.* The only profile variable is
   =${project_root}=; there is no =${session_dir}=. Profiles are project-centric:

   | profile      | READ | WRITE              | DENY                                   |
   | coding       | /**  | ${project_root}/** | .git, runtime, .env, node_modules      |
   | bash-enabled | /**  | ${project_root}/** | (same)                                 |
   | research     | /**  | (none, read-only)  | (same)                                 |
   | restricted   | (none)| (none)            | (same)                                 |
   | system-expl  | /**  | (none)             | (same + .ssh, .key, .pem, shadow, ...) |

   Agents don't use a profile: =jf/gptel-persistent-agent--build-scope-plist=
   hardcodes =read=allowed_paths=, =write=("/tmp/**")=, standard deny.

** What workspaces changes (the target paradigm)

=config/workspaces/= (newly merged) makes the *work root a first-class,
on-disk concept*. A workspace = a named context, 1:1 with a tab, anchored
to an absolute =:home= directory. gptel sessions now live *inside* it.

User-confirmed topology: *worktrees are colocated in the workspace dir as
siblings; the workspace =:home= IS the project dir.*

#+begin_example
  ~/emacs-workspaces/<name>/                  :home  ==  WORK ROOT (Role 2) == project dir
  ├── home.org                                dashboard; editable; #+TITLE overrides display name
  ├── .git/
  ├── sessions/                               gptel sessions live HERE (Role 1, inside :home)
  │   └── <id>/branches/main/session.org
  │       └── agents/<agent>/session.org      agent sandbox, also inside :home
  ├── <worktree-feature-a>/  (.git is a FILE) ┐ sibling worktrees, enumerable
  └── <worktree-feature-b>/  (.git is a FILE) ┘
#+end_example

Key consequences:
- =${project_root}= now expands to =:home= (=workspace-integration.org:85=
  passes =project-root=home= into =create-session-core=). So *Role 3 (scope)
  is correct by construction in this path*: =WRITE: <:home>/**= covers
  sessions, worktrees, everything. The part feared broken isn't, in this flow.
- Of the three roles, workspaces solves two (metadata under :home, scope
  rooted at :home). *Only Role 2 (work root / default-directory) is still
  on the floor.*

Workspaces seam facts (one-way, decoupled):
- gptel consults =workspace-sessions-dir= behind =(featurep 'workspaces)= +
  =fboundp= guards (=filesystem.org:76-91=); =jf/gptel-persistent-session-global=
  forces global =~/.gptel/sessions/=.
- Integration registry: =workspace-register-integration= with =:on-create= /
  =:menu= / =:on-purge=; anchor payload plist =(:name :home :sessions-dir :context)=
  (=integrations.org=). gptel registers =gptel-session= in
  =workspace-integration.org= under =with-eval-after-load 'workspaces=.
- Workspaces ALREADY enumerates sibling worktrees:
  =jf/workspace--worktree-children= (=git-worktrees.org:235=) returns
  =(:dir :branch :source)= plists. (NB: that helper is workspaces/magit-coupled;
  =git worktree list= / =magit-list-worktrees= is the neutral alternative.)

** The path-archaeology problem (the rotten core)

Session identity + binding is reverse-engineered from the filesystem path
on every file open. User strongly dislikes this; it is fragile to any
reorg (workspaces is one of many).

- =jf/gptel--auto-init-session-buffer= on =find-file-hook= — runs on *every
  file open* (=commands.org:932=, comment at =:247=). Checks filename ==
  "session.org", then tries 3 hardcoded layout regexes (branch / nested-agent
  / flat-agent, =commands.org:270-309=), walks up with =../..= / =../../..=.
- Identity IS the directory basename: =jf/gptel--session-id-from-directory=
  = =(file-name-nondirectory dir)= (=filesystem.org:124-127=). Nothing stored;
  rename/move the dir -> identity silently changes.
- This hook does two jobs badly (binding + identity) and ignores a third
  (work root). Scope (Role 3) already avoids archaeology by being
  drawer-resident — the model A and B should copy.

** Activation mechanics already lean toward idiomatic (grounding for Thread A)

- =gptel-chat-mode= is a real major mode: =(define-derived-mode gptel-chat-mode
  org-mode ...)= (=mode.org:963=).
- =jf/gptel--ensure-mode-once= (=commands.org:113-122=) already anticipates
  external activation: comment says "when =auto-mode-alist= or a mode cookie
  already brought it up." The global find-file-hook is treated as a *redundant
  fallback*.
- =gptel-chat-mode-hook= already owns substantial buffer setup (drawer fold,
  startup visibility, display activate, preset application) and there is
  already a =hack-local-variables-hook= pass for file-local preset declaration
  (=menu.org:895=; dir-locals discussed =menu.org:113-148=). So local-variables
  / dir-locals is *not foreign* here — it is partially present.

* Emerging design principle (converged, not yet ratified)

# CORRECTION (2026-06-17 session): the "Preamble source in dir-locals" leg
# below is SUPERSEDED -- see [[*RESOLVED C]]. The preamble-source pointer goes
# in the DRAWER (written at create time from the integration-registry
# payload), not dir-locals. Also: the parenthetical "dir-locals can't set
# default-directory" (last bullet of this section) is FALSE -- verified by
# batch experiment that it can; see Thread C "Verified Emacs mechanics".
# The "Work root in the drawer" leg STANDS and is now reinforced.

A single rule resolves the storage/activation questions and honors the
workspaces-decoupling constraint:

#+begin_example
  DRAWER (:PROPERTIES:)   per-ENTITY · varies · authoritative · co-located
    GPTEL_WORK_ROOT (cwd)      -> set default-directory from this in the mode hook
    GPTEL_SCOPE_* (already)    -> MUST agree with cwd, so keep them together
    [identity, if promoted off the path]
    written by whoever creates the session/agent (workspace OR plain cmd OR manual)

  DIR-LOCALS (.dir-locals.el)  per-TREE · shared · external-producer-writable
    jf/gptel--preamble-source  -> workspace points it at home.org; cascades down
    (optionally) mode activation for a whole tree
    inheritance is a FEATURE here (all sessions in a workspace share it)

  MAGIC-MODE-ALIST             activation only · content-addressed
    recognize the drawer signature (:GPTEL_PRESET: ...) -> gptel-chat-mode
    -> mode hook does all setup; RETIRES the global find-file-hook + path regexes
#+end_example

Principle in one line: *dir-locals where inheritance helps (shared tree
config), drawer where inheritance hurts (per-entity values that vary),
content-addressing for recognition (so filesystem layout stops mattering).*

Why each:
- *Work root in the drawer, not dir-locals:* dir-locals cascade downward, and
  agents live *under* the parent session dir — they would inherit the parent's
  cwd, but agents need a *different* cwd (a specific worktree). Per-entity
  values that must agree (cwd <-> scope) belong together in the drawer, which is
  already per-agent and authoritative for scope.
- *Preamble source in dir-locals:* the value is set by an external producer
  (workspace drops =.dir-locals.el= at =:home= pointing at =home.org=) and
  inherited by the whole tree. gptel reads buffer-local =jf/gptel--preamble-source=
  and never names a workspace symbol — textbook inversion of control,
  satisfying the decoupling constraint. Absent workspaces -> default preamble.
  (Mark vars =safe-local-variable= to avoid prompts.)
- *magic-mode-alist for recognition:* a session is recognized because it
  *contains* a gptel drawer, not by path or name. Same self-describing
  principle scope already uses, extended to activation. Once the mode activates
  by any means, the mode hook is the single setup point (and can set
  =default-directory= after find-file has run, sidestepping the "dir-locals
  can't set default-directory" edge).

* Threads (follow-up session seeds)

Each is a candidate standalone session. Status keywords: THREAD = open
design work; PARKED = deferred; SEPARABLE = independent / janitorial.
Several are cross-cutting and may collectively warrant promotion to an
openspec change once their shapes settle.

** RESOLVED A — Binding & activation: retire the find-file-hook archaeology
   :PROPERTIES:
   :THREAD_SCOPE: gptel/sessions (chat mode activation, buffer binding)
   :END:

Replace the global =find-file-hook= + 3 path regexes + =../..= walks with
content-addressed activation, so identity/binding stop depending on
filesystem layout.

Direction: =magic-mode-alist= keyed on the drawer signature (e.g.
=:GPTEL_PRESET:= near buffer head) -> =gptel-chat-mode=; the mode hook
establishes all buffer context. File-local =-*- mode: gptel-chat -*-= cookie
and dir-locals mode-for-tree are secondary options.

Open questions:
- Does =magic-mode-alist= co-exist cleanly with org-mode for =.org= files
  carrying the signature? (gptel-chat-mode *is* org-mode +, so likely fine —
  confirm.) Can the signature regex false-match a normal org file that merely
  *quotes* =:GPTEL_PRESET:=? Anchor it to a real drawer.
- Keep "open session.org in dired and it just works" (auto-adoption), or accept
  that activation now requires the signature/cookie? User's appetite for
  dropping global auto-adoption is the key unknown.
- Safety prompts: dir-locals =eval=/mode and non-safe vars prompt unless marked
  =safe-local-variable=. File-local =mode:= cookie is generally smooth.

Code: =commands.org:225,247,270-309,932= (hook+regexes), =commands.org:113-122=
(=ensure-mode-once=, already anticipates cookies), =mode.org:963= (derived mode),
=mode.org / display.org / menu.org= (=gptel-chat-mode-hook= setups),
=menu.org:895= (=hack-local-variables-hook=).

** RESOLVED B — Work root in the drawer; set default-directory in the mode hook
   :PROPERTIES:
   :THREAD_SCOPE: gptel/sessions + gptel/scope (Role 2)
   :RESOLVED:  2026-06-17 explore session (design locked + spike confirmed)
   :END:

Give Role 2 an explicit home: a =GPTEL_WORK_ROOT= drawer key, set
=default-directory= from it in the mode hook. Co-locate with =GPTEL_SCOPE_*=
because cwd and scope must agree. This is the *core* fix for the cwd bug and
what makes scope validation meaningful/testable.

*Architecture shift since the doc was written:* Threads A and F are
implemented. Activation is content-addressed (=magic-mode-alist= on the
drawer signature, =mode.org:1088=); the *single* setup seam is the mode-hook
binder (=commands.org:~260-300=), which ALREADY scans the drawer alist
(=jf/gptel--scan-session-drawer-keys=) and =setq-local='s four session vars.
So the RESOLVED-C ordering worry (mode-hook vs =hack-local-variables=) does
NOT apply here — that was a dir-locals concern; the drawer is read directly
at mode-hook time. Adding =default-directory= is one =setq-local= beside the
existing four.

*** Locked design (chat sessions)

- WRITE (=create-session-core=, =commands.org:~544=): =project-root= is the
  SINGLE input; persist it verbatim as =:GPTEL_WORK_ROOT:= via
  =jf/gptel--append-drawer-property= (same splice the identity keys use). One
  input -> two outputs (=GPTEL_SCOPE_*= expanded + =GPTEL_WORK_ROOT= verbatim);
  the cwd<->scope agreement invariant is then STRUCTURAL, not enforced.
  Resolves cross-cutting Q3: work-root is neither a rename nor an independent
  concept — it is a second *persisted output* of the project-root input.
- READ (binder, =commands.org:~280=, its own commented stanza beside the four
  =setq-local='s, BEFORE the =condition-case= registry block so it survives a
  registry failure):
    =(setq-local default-directory
       (file-name-as-directory
        (expand-file-name
         (or (cdr (assoc "GPTEL_WORK_ROOT" drawer-alist)) branch-dir))))=
- Decisions (2026-06-17, all reversible under beta/no-migration):
  | fork              | locked choice                                            |
  | work-root model   | derived output of one input (project-root)               |
  | keyless fallback  | =branch-dir= (= today's find-file value; no-op for legacy)|
  | storage form      | absolute, verbatim (matches =${project_root}= expansion) |

*** Load-bearing spike — CONFIRMED (2026-06-17, traced gptel-request.el)

Question: do INTERACTIVE chat tool calls (not just agents) run with the chat
buffer's =default-directory=? If not, a buffer-local set in the binder would
miss chat. Answer: YES.
- =gptel-request= (=:1773=): =:buffer= defaults to =(current-buffer)= at call
  time, stored as =info :buffer= (=:1982=).
- =gptel--handle-tool-use= (=:1692=): wraps the ENTIRE tool-funcall loop in
  =(with-current-buffer (plist-get info :buffer) ...)=; both async (=:1740=)
  and sync (=:1744=) =apply= the tool fn INSIDE that binding.
- So validation AND execution run with the originating buffer's
  =default-directory=. Chat: user sends from the chat buffer. Agent: =--task=
  sends inside =(with-current-buffer agent-buffer ...)=. Both covered by ONE
  buffer-local set in =gptel-chat-mode-hook=. No second binding point needed.

Mechanism confirmed end-to-end: filesystem tools (=scope-filesystem-tools.org:
81,124,172=, bare =expand-file-name=) and bash (=scope-validation.org:299,420=,
resolves against =default-directory=) both pick up the binder's value.

*** Scope check — DONE (2026-06-17, this branch; supersedes the .tasks caveat)

The doc warned the worked example's rows were captured on another worktree
and "not to be trusted as the current baseline." Re-run on THIS branch:
- =config/gptel/scope/test/validation= suite: 195/195 green.
- Focused absolute-path batch experiment (full isolated init), ALL PASS:
  | scenario (absolute paths)                       | result                       |
  | load-config(missing dir)                        | deny-all-defaults (all nil)  |
  | deny-all + any read                             | DENY =not-in-scope=          |
  | read inside read scope (/etc/hosts)             | ALLOW                        |
  | write inside write scope (<root>/src/x.el)      | ALLOW                        |
  | write OUTSIDE write scope (/tmp/elsewhere.el)   | DENY =not-in-scope=, and the |
  |                                                 | denial carries =:allowed-    |
  |                                                 | patterns ("<root>/**")= —    |
  |                                                 | the exact expansion offer    |
  | read of write-only path (read>=write hierarchy) | ALLOW                        |
  | write <root>/sub/.git/config (deny precedence)  | DENY =denied-pattern=        |
CONCLUSION: fail-CLOSED confirmed (no fail-open path); denials carry the
allowed-patterns the expansion flow offers; deny beats write-allow. The
machinery is meaningful on absolute paths TODAY — so once B fixes
=default-directory=, RELATIVE paths will resolve into this same (verified)
boundary. The cwd fix is the only missing piece, not the validator.

Code: =commands.org:~544= (=create-session-core=, write), =commands.org:~280=
(binder, read), =persistent-agent.org:560-620= (agent drawer write — see
[[*THREAD E]]), =scope-validation.org:147-219,299,420= (resolution),
=scope-filesystem-tools.org:81,124,172=, =scope-shell-tools.org:127,215=,
=gptel-request.el:1692,1740,1744,1773,1982= (tool-call buffer context).

** RESOLVED C — dir-locals seam subsumed by the integration-registry payload
   :PROPERTIES:
   :THREAD_SCOPE: workspaces + gptel/sessions (preamble source, IoC)
   :RESOLVED:  2026-06-17 explore session
   :END:

Original framing (now superseded): workspaces drops a =.dir-locals.el=
at =:home= setting =jf/gptel--preamble-source= (and other tree-shared
hints); the value cascades to every session under the tree; gptel reads
a buffer-local and never names a workspace symbol.

*Resolution: do NOT add a dir-locals layer. Write gptel's values into
the drawer at create time, sourced from the integration payload.*
Thread C largely collapses into [[*THREAD B]].

*** Why: two candidate seams, and one already exists

#+begin_example
  SEAM 1 — integration registry        (PUSH, at create time)   ALREADY EXISTS
    workspaces --(payload :home ...)--> gptel :on-create --> gptel writes drawer
    - workspaces names no gptel symbol     - gptel is the sole writer
    - value delivered exactly when gptel is creating the session

  SEAM 2 — .dir-locals.el at :home      (PULL, at open time)     what C proposed
    gptel :on-create --> writes .dir-locals.el --> gptel reads it on every open
    - gptel writes AND reads it (per "only gptel touches gptel vars")
#+end_example

Once we concede only gptel writes/reads its own variables, Seam 2's
writer and reader are *both gptel* -> dir-locals is not a decoupling
device, just a store gptel writes to itself. *The decoupling was always
coming from the registry payload, not from dir-locals.* Seam 2 is a
redundant second seam.

The only structural difference is push-at-create vs pull-at-open
(write-once-per-tree vs write-once-per-session). That pays off ONLY for
sessions gptel's create path never ran for:

  | session source           | covered by drawer (Seam 1)?      |
  |--------------------------+----------------------------------|
  | :on-create / :menu       | yes                              |
  | agent (--task)           | yes (needs own value anyway)     |
  | branch                   | yes (inherits parent drawer)     |
  | hand-authored / external | NO -- only dir-locals catches it |

Every row but the last funnels through gptel code already holding
=:home=. So the dir-locals-only benefit reduces to "a tree default for
sessions gptel never created" -- essentially the hand-authored case.

Cost of adding Seam 2: a git-committed artifact; merge-with-the-user's-
existing-=.dir-locals.el=; cascade noise into agent subdirs; and a new
*silent* fallback (an agent whose drawer write fails inherits =:home=
instead of its worktree -- reviving the cwd/scope-disagreement bug class
without a symptom).

*** Verified Emacs mechanics (batch experiments, 2026-06-17)

These overturn two claims made elsewhere in this doc (correction note
under [[*Emerging design principle]]):

- dir-locals *can* set =default-directory= (the doc's "can't" is FALSE).
- =default-directory= is =risky? nil=, =safe? stringp= in stock Emacs:
  settable, no prompt.
- Ordering is fixed: =gptel-chat-mode-hook= runs BEFORE file/dir-locals
  are applied (sees the file's own dir); =hack-local-variables-hook= runs
  AFTER (sees forced values). The codebase already relies on this split
  (=menu.org:895=, preset case).
- A drawer-driven override in =hack-local-variables-hook= BEATS a
  dir-locals value (a deep-subdir "agent" buffer overrode the inherited
  tree value). So dir-locals + drawer is a PRECEDENCE STACK, not XOR --
  this dissolves the doc's original objection (agents "would inherit the
  parent's cwd") but does NOT make the layer worth adding.

*** What dir-locals uniquely does (and why none of it serves a gptel goal)

A drawer only exists in files gptel authored and only means anything
once gptel's mode+hook are live. dir-locals is applied by Emacs core to
ANY file under the tree. That gap is real; the full inventory:

  | # | dir-locals-only capability                          | serves a gptel goal?               |
  |---+-----------------------------------------------------+------------------------------------|
  | 1 | reaches non-drawer buffers (src, home.org, dired)   | NO -- gptel must not touch those   |
  | 2 | retroactive: edit file -> existing sessions on open | marginal (beta/no-migration)       |
  | 3 | computed/self-relocating values via eval (move-safe)| marginal (eval risky; abs-anchored)|
  | 4 | single source of truth for tree (vs N drawers)      | low (values create-time-stable)    |
  | 5 | subtree override topology (.dir-locals.el in subdir)| NO -- drawer per-ENTITY is better  |
  | 6 | idiomatic/discoverable location                     | soft/UX only                       |

Every unique capability either serves a non-goal (1), is modelled
better by the drawer (5), or is marginal under beta/no-migration +
create-time-stable values (2,3,4).

*** The reframe that keeps it clean

Capability #1 IS legitimately valuable -- "working anywhere in this
workspace sets my environment" -- but that is a *workspace-level* goal
about *non-gptel* buffers with *non-gptel* values (=default-directory=,
=compile-command=, project settings). gptel declining dir-locals does
NOT take that away: workspaces may drop its OWN =.dir-locals.el= with
non-gptel values anytime, naming no gptel symbol, fully decoupled.

#+begin_example
  gptel values (work-root, scope, preamble-pointer)  -> drawer via payload         no dir-locals
  workspace values (default-directory tree-wide ...)  -> workspaces' OWN .dir-locals  optional
#+end_example

*** Deferred escape hatch + re-entry condition

The precedence stack (drawer overrides an inherited dir-locals default)
is PROVEN to compose safely. Keep it in the back pocket: if hand-
authored sessions or a user-set workspace-wide default ever become worth
supporting, a dir-locals tree default can be added WITHOUT disturbing the
drawer-first design. It is an extension point, not the architecture.

Re-entry condition (the push-vs-pull boundary): the registry payload is
a PUSH seam (fires only at create). The day gptel gains a value that must
reflect the *current* tree state at every open -- rather than the state
frozen at create -- a pull seam (dir-locals or discovery) re-earns its
place. Checked 2026-06-17: work-root, scope, and the preamble-source
pointer are all create-time-stable; preamble *content* is resolved at
build time from whatever file the (stable) pointer names. Nothing needs
pull today.

Code: =workspace-integration.org:66-87= (=:on-create=, payload =:home=),
=integrations.org= (registry, anchor payload), =menu.org:113-148,895=
(existing dir-locals cooperation + the two-hook ordering), README
"gptel sessions" + "Integration registry".

** RESOLVED D — Dynamic environment block (TAIL of the composed system message)
   :PROPERTIES:
   :THREAD_SCOPE: gptel/sessions or gptel/chat (system prompt / preamble)
   :RESOLVED:  2026-06-19 explore session (home.org leg dropped; injection
               + placement + composition locked)
   :END:

Tell the model its environment (work-root, scope, available worktrees) so it
need not run discovery tool calls. User: "the more offered in the preamble the
better, at least until it gets long."

*** Adjustment that simplified the whole thread (2026-06-19): drop home.org

The original framing layered a workspace-neutral base + an OPTIONAL =home.org=
override, which forced =home.org= to be "override only, never source" and made
the builder merge two sources with different trust/availability. *Dropping the
home.org leg entirely* makes the builder PURE: every input is intrinsic to the
session and workspace-neutral.

#+begin_example
  ENV BLOCK (dynamic) — 100% workspace-neutral builder
    work-root  <- drawer GPTEL_WORK_ROOT   (already set; Thread B done)
    scope      <- drawer GPTEL_SCOPE_*      (read live; MUTATES mid-session)
                                          ^
     workspaces' ONLY contribution: it wrote GPTEL_WORK_ROOT at :on-create.
     After that gptel reads its OWN DRAWER; names no workspace symbol;
     works identically with workspaces absent.
#+end_example

*Worktree list DROPPED from scope (2026-06-19):* an earlier draft of this
block also listed available worktrees via =git worktree list=. Pulled out —
it raises discovery/selection questions (which worktrees, how surfaced, how
the parent chooses one) that belong with Thread E's manifest question, not in
the env-block MVP. The env block ships with work-root + scope only.

*** Two grounding facts that shaped the design (2026-06-19)

1. *The injection seam already exists and already covers agents.* The pre-send
   refresh is a =:before= advice on =gptel-request= (=menu.org:446=) that
   re-reads the sibling =system-prompt.<ext>= into =gptel--system-message=,
   gated to chat-mode buffers (=menu.org:428=). Agents send via =gptel-request=
   from a buffer that IS =gptel-chat-mode= (=persistent-agent.org:543-545=,
   =692-706=), so the SAME advice fires for them. One seam covers chat + agents.
2. *Scope is mutable mid-session.* =request_scope_expansion= PERSISTS new
   patterns into the drawer (=scope-expansion.org= collapses ops into
   =GPTEL_SCOPE_*= keys). A frozen scope summary would go stale — so the env
   block genuinely wants per-send recomputation, which is exactly the cadence
   the pre-send seam runs at. (Scope is the volatile field that earns per-send
   rebuild; with worktrees dropped, work-root is otherwise create-time-stable.)

*** Locked design

- *Placement = TAIL.* Append the env block AFTER the sibling body:
  #+begin_example
    gptel--system-message  <-  sibling-body  +  "\n\n"  +  env-block

    agent:  [harness preamble + role]  + [env]    <- sibling stays baked, untouched
    chat:   [role]                     + [env]
  #+end_example
  TAIL was chosen over "env between harness and role" specifically because the
  agent sibling is =harness + role= BAKED TOGETHER at create
  (=persistent-agent.org:350-360=); inserting env BETWEEN them would require
  splitting that blob (a known-prefix splice hack) OR moving the harness to
  send-time composition (would unify the two sibling writers + make the harness
  live, but the sibling stops being the self-contained full prompt, and needs an
  agent-type signal at send). TAIL sidesteps all of it: pure =concat=, *agent
  write path untouched, sibling stays self-contained.* Order the model reads:
  identity -> rules -> role -> "here's your current environment" (recency favors
  the volatile data being last).
- *Channel = system message* (not a context turn): the data is framing, not
  conversational, and the seam already produces the system message.
- *Idempotent wholesale rebuild* (the load-bearing invariant): each send sets
  =gptel--system-message= FRESH from =(sibling-file-body + recomputed-env)=,
  and NEVER reads its own prior buffer-local value. Both halves are rebuilt from
  source (file + drawer) every send -> no accumulation, no staleness. The
  existing refresh already rebuilds the role half wholesale from the FILE
  (=menu.org:431-435=); env appends the same way.
- *One seam, env append is UNCONDITIONAL.* The sibling refresh no-ops when the
  drawer has no =:GPTEL_SYSTEM_PROMPT_FILE:= (=menu.org:429=), but EVERY session
  must get the env block. So: (1) role-content = sibling body (conditional,
  defensive; else the already-installed =gptel--system-message=), (2) env-block
  = built from the drawer (UNCONDITIONAL), (3) =gptel--system-message= =
  compose(role, env) (wholesale set). One widened function or two ordered
  =:before= advices (env MUST run after the sibling read) — implementation call
  for design.
- *Content (Thread C's old length question, now scoped):* 1-2 sentences of prose
  explaining what the env info is + the facts (work-root, scope) + a sentence
  noting the info is dynamic / live / current. (Worktrees dropped, see above.)
- *Graceful degradation:* a chat-mode buffer with no drawer (plain chat) still
  hits the advice — report =default-directory= and "no scope restrictions / not
  a scoped session" rather than erroring or emitting an empty block.

*** Still open / flagged (not blocking)

- *Length budget:* scope verbatim globs vs summarized. User: "more is better
  until it gets long." Soft.
- *Worktree manifest:* DEFERRED (dropped from this block's scope, see above).
  Lives with Thread E's open manifest question.

Code: =menu.org:375-448= (pre-send refresh advice, the seam), =menu.org:312-372=
(=system-prompt-file-path= / =apply-system-prompt-file=), =commands.org:262-317=
(binder; =default-directory= from =GPTEL_WORK_ROOT=), =persistent-agent.org:
273-360= (harness preamble baked into the agent sibling), =scope-expansion.org=
(scope mutates the drawer mid-session), preset =.md= files.

** RESOLVED E — Dynamic agent cwd & scope (mechanism SHIPPED; env block surfaces it)
   :PROPERTIES:
   :THREAD_SCOPE: gptel/tools/persistent-agent + gptel/scope
   :RESOLVED:  2026-06-19 explore session (agent leg found already implemented;
               D<->E composition locked; write-perms taken as an assumption;
               worktree manifest the lone deferred item)
   :END:

User: agent scope and default-directory must be *dynamic*; the typical case is
a *specific worktree*. The agent gets its OWN drawer carrying its own
=GPTEL_WORK_ROOT= and scope, chosen per spawn — self-describing, not inherited.

*** The agent leg is ALREADY IMPLEMENTED (archive 2026-06-18-gptel-work-root-default-directory)

What this thread proposed as "locked design" SHIPPED in the work-root change
(decisions D5/D6/D7). Verified in =persistent-agent.org= + tangled =.el=:
- =--task= takes =work_root=, =read_paths=, =write_paths=
  (=persistent-agent.org:521-523=); =allowed_paths= removed (D6).
- =work_root= written to the agent's OWN drawer =:GPTEL_WORK_ROOT:= (=:680=);
  omitted ⇒ defaults to the parent buffer's =default-directory=, FROZEN at
  create (=:578-583=, NOT a live link). The shared binder ([[*RESOLVED B]])
  reads it and sets =default-directory=.
- =build-scope-plist= split read/write; =/tmp/**= appended as scratch (=:202=).
- Coupling guardrail evolved: the old warn-on-mismatch (D7) was SUPERSEDED by
  auto-include — =work_root/**= is PREPENDED to the agent's READ scope so the
  work root is "readable by construction" (=:598-603=, commit b62b7e38).

*** The clarification: the env block (Thread D) IS the agent's cwd/scope channel

The original framing said E must "tell the agent its cwd/scope." It doesn't
need bespoke work: *agents are =gptel-chat-mode= buffers with their OWN drawer*,
so [[*RESOLVED D]]'s per-send env builder applies to them unchanged.

#+begin_example
  PARENT spawns agent
     | --task writes the agent's OWN drawer: GPTEL_WORK_ROOT + GPTEL_SCOPE_*
     v
  AGENT buffer (gptel-chat-mode)
     | binder:   default-directory <- GPTEL_WORK_ROOT       (shipped; B/E)
     | pre-send: env block         <- agent's OWN drawer     (D, same builder)
     v
  agent's system-message TAIL: "work root <X>; read/write <scope>; live."
#+end_example

So D SUBSUMES the "communicate the agent's environment" half of E: one per-
buffer, drawer-sourced builder; no agent-specific env code. Agent env block ==
chat env block. The agent-specific *framing* ("you are a headless sub-agent")
stays in the static harness preamble — a separate layer from the env block.

"Dynamic" in the way that matters: if an agent grows scope via
=request_scope_expansion= mid-run (drawer mutates), the per-send env block
reflects the new scope next turn. cwd stays frozen (an agent should not
relocate mid-task).

*** Assumption taken for this work (2026-06-19)

*Agents will have appropriate WRITE permissions* — i.e. the parent-passed
=work_root= ends up in the agent's WRITE scope (not just read). Tracked
separately as a real gap: the shipped auto-include landed on the READ axis
while the original silent-DENY problem is on the WRITE axis (relative writes
under =work_root= still deny). The validator's read<=write hierarchy
(=scope-validation.org:188-193=) means putting =work_root/**= in WRITE makes it
both writable AND readable, subsuming the read prepend. The change's OWN spec
already requires it (=specs/scope/spec.md:28-33=, "relative write inside the
work root is in scope by construction" — true for chat via the coding profile's
=write: ${project_root}/**=, FALSE for agents today). Full drift diagnosis +
fix recorded in =.tasks/agent-work-root-into-write-scope.md= (separate small
item, per the 2026-06-19 boundary decision). For preamble/E purposes we ASSUME
it fixed.

*** The lone deferred item

- *Worktree MANIFEST source* for the parent to CHOOSE a non-default =work_root=
  (the worktree-isolation case). Dropped from the env block ([[*RESOLVED D]]).
  Until it lands, the parent reaches a non-default root three ways, all working
  today with what is shipped: (a) parent-default (omit ⇒ agent works in the
  parent's tree), (b) explicit-from-user, (c) on-demand =git worktree list= via
  a tool call. Keep workspace-neutral when built. This is the only thing
  between "agent works where the parent is" and "parent fans agents across
  isolated worktrees."

Code: =persistent-agent.org:521-710= (=--task=; work_root + read/write paths;
read auto-include =:598-603=; drawer write =:680=), =build-scope-plist= =:202=,
=scope-validation.org:188-193= (read<=write hierarchy), =scope-expansion.org=
(scope mutates the drawer mid-session), archive
=2026-06-18-gptel-work-root-default-directory/= (design D5/D6/D7, specs).

** RESOLVED F — Identity: stop deriving it from the path
   :PROPERTIES:
   :THREAD_SCOPE: gptel/sessions
   :END:

The last vestige of archaeology: =session-id= = dir basename
(=filesystem.org:124-127=). Once activation (A), cwd (B), and scope no longer
depend on the path, decide whether to *store* identity (a =GPTEL_SESSION_ID=
drawer key -> rename/move-safe) or accept path-derived identity as harmless.

Open questions:
- Does anything rely on id == basename (registry keys, symlinks, discovery)?
  Audit =registry.org=, =filesystem.org= discovery (=:247,260,285=), branching.
- If stored: migration for existing sessions (or beta "no migration" stance).

Code: =filesystem.org:124-127,247,260,285=, =registry.org:66=, =branching.org=.

** SEPARABLE — Thread G — Reconcile request_scope_expansion response shapes
   :PROPERTIES:
   :THREAD_SCOPE: gptel/scope (shell + filesystem expansion)
   :END:

Two shapes/semantics from one tool in one session: filesystem path returns
={success, patterns_added:[...]}= (persistent pattern add); bash path returns
={success, allowed_once:true}= (allow-once). Reconcile. Independent of the
directory work; can proceed in parallel. Area churned recently — see
=.tasks/done/refactor-request-scope-expansion-to-take-operation.md=.

Code: =scope-shell-tools.org= (~263), =scope-expansion.org= (~700 add, ~746
allow-once), =scope-validation.org:1055-1065= (callback parses =:allowed_once=).

** SEPARABLE — Thread H — Delete dead tools.org / system-prompts.org plumbing
   :PROPERTIES:
   :THREAD_SCOPE: gptel/sessions (janitorial)
   :END:

Pure cleanup, zero behavior change. =jf/gptel-session--tools-log-file=
(=constants.org:77=) + =jf/gptel--tools-log-path= (=filesystem.org:175=) have
no call sites; =system-prompts.org= constant (=constants.org:81=) likewise.
Remove constant + accessor together. Good warm-up task / first PR.

* Cross-cutting constraints & invariants

- *Decoupling (hard):* gptel's work-root/default-directory/preamble setup must
  work with workspaces absent. Workspaces is one *producer* (writes drawer
  values at =:on-create=, drops =.dir-locals.el=); gptel is a consumer of
  intrinsic per-session data + buffer-locals. This *eliminates* "consult the
  live workspaces registry at runtime" as a work-root source.
- *Self-describing sessions:* prefer mechanisms where context travels with the
  session (drawer content; content-addressed activation) over mechanisms that
  reverse-engineer it from the path.
- *cwd must agree with scope:* whatever sets =GPTEL_WORK_ROOT= must keep it
  consistent with =GPTEL_SCOPE_*= (relative paths resolve into the scoped tree).
- *Beta, no-migration default:* on-disk formats may break between cycles; a real
  migration story is reserved for beta (mirrors workspaces persistence policy).

* Open cross-cutting questions (span multiple threads)

1. Auto-adoption: keep "open any session.org -> it activates," or require a
   content signature? (Thread A primary; affects F.)
2. One store vs two: RESOLVED (2026-06-17, [[*RESOLVED C]]) -> everything
   gptel owns goes in the DRAWER; the IoC seam is the integration-registry
   payload, NOT dir-locals (which would be a redundant second seam). (A/B/C.)
3. Is =project-root= -> =work-root= a rename of the existing (discarded)
   =project-root= parameter, or a new persisted concept beside it? (B/E.)
4. Preamble length budget and what gets trimmed first. (D — env block now
   RESOLVED at TAIL via the pre-send seam; length remains the one soft-open
   sub-question, see [[*RESOLVED D]] "Still open / flagged".)

* Code reference index (file:line)

- =config/gptel/sessions/commands.org=
  - =113-122= =ensure-mode-once= (anticipates cookie/auto-mode)
  - =225,247= =auto-init-session-buffer= + "runs on every file open"
  - =270-309= three layout regexes + =../..= walks
  - =345-348= sets buffer-local session vars
  - =544= =create-session-core= (takes project-root; never sets default-directory)
  - =783,800= project-root from first selected projectile project
  - =932= =(add-hook 'find-file-hook #'jf/gptel--auto-init-session-buffer)=
- =config/gptel/sessions/filesystem.org=
  - =76-91= =workspace-sessions-dir= consult (featurep/fboundp guard)
  - =124-127= =session-id-from-directory= (= dir basename)
  - =175= =tools-log-path= (DEAD)
  - =247,260,285= session discovery roots
- =config/gptel/sessions/constants.org= =33= sessions-directory; =77= tools-log-file (DEAD); =81= system-prompts.org (DEAD)
- =config/gptel/sessions/workspace-integration.org= =66-87= =:on-create= (project-root=home); =118-135= =:menu=; =144-148= registration
- =config/gptel/scope/scope-validation.org= =123= glob match; =147-219= validate-path-operation; =680-698= deny-all-defaults; =724-759= load-config (fail-closed); =876-951= authorize-tool-call; =1055-1065= expansion callback parse
- =config/gptel/scope/scope-filesystem-tools.org= =81,124,172= expand-file-name (read/write/edit)
- =config/gptel/scope/scope-shell-tools.org= =49-53= absolute-path warning; =127,215= default-directory use; ~263 expansion shape
- =config/gptel/scope/scope-expansion.org= ~700 patterns_added; ~746 allowed_once
- =config/gptel/scope-profiles/*.yml= ${project_root} only; coding read /** write ${project_root}/**
- =config/gptel/tools/persistent-agent.org= =202-217= build-scope-plist; =514+= =--task=; =597= find-file-noselect; tool =args= schema; standard-deny-paths
- =config/gptel/chat/mode.org= =963= define-derived-mode (org-mode); gptel-chat-mode-hook setups
- =config/gptel/chat/menu.org= =113-148= dir-locals cooperation; =895= hack-local-variables-hook
- =config/workspaces/integrations.org= registry + anchor payload (:name :home :sessions-dir :context)
- =config/workspaces/data-model.org= =278-298= =workspace--home=; =429-433= =workspace--sessions-dir=
- =config/workspaces/git-worktrees.org= =235-261= =worktree-children= (:dir :branch :source)
- =config/workspaces/docs/README.org= concepts, gptel sessions routing, integration registry, home.org
